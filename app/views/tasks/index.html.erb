<div class="wrapper">
  <div> <h1>Your Tasks</h1></div>

  <div class="container">
    <ul><% @tasks.each do |task|%>
      <li><%=link_to task.name, task_path(task) %></li>
    <%end%>
    </ul>
  </div>
</div>

